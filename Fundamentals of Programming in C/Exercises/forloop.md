### Exercise: Calculate 10!

#### Question

Use a `for` loop to calculate the factorial of 10 using the values
stored in the array.

#### What I Learned

I learned how to use a `for` loop to go through each element of an
array and perform a calculation.

```c
for (i = 0; i < 10; i++) {
    factorial = factorial * array[i];
}