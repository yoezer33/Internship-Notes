### Exercise: Find Even Numbers Using a While Loop

#### Question

Use a `while` loop to check the values in the array and print the
even numbers.

#### What I Learned

I learned how to use a `while` loop to go through an array and how
to use `%` to check whether a number is even.

```c
if (array[i] % 2 == 0)